name: flutter_graphql_sample
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  gql_http_link: ^0.4.5 # 追加
  ferry_flutter: ^0.8.0+1 # 追加
  ferry: ^0.14.0+1 # 追加

  flutter_secure_storage: ^8.0.0 # 追加

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  ferry_generator: ^0.8.0+1 # 追加
  build_runner: ^2.3.3 # 追加

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
